let implicitString  = 'Explicitly infered';

let explicitString:string = 'Explicitly infered';

let frameworks  = ['angular', 'React', 'Ember', 'BackBone', 'Vue'];

function implicitNumberReturn () {
	/* logic goes here */
	return 1;
}

function explicitNumberReturn () :number {
	/* logic goes here */
	return 1;
}

